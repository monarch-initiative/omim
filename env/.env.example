# Optional LinkML overrides. Prefer root .env (API_KEY) like legacy/CI.
# OMIM_API_KEY=           # alias for API_KEY if unset
# OMIM_MAX_MIMS=1000      # local test cap
# OMIM_SLEEP_SECONDS=0.5  # pace requests if many 429s
